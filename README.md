Designing Buck-Boost

## Setup
To run this project, create a virtual environment and install dependencies:
\```bash
python -m venv venv
source venv/bin/activate  # On Windows use: venv\Scripts\activate
pip install -r requirements.txt
\```